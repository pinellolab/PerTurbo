[envs.default]
dependencies = [
    "perturbo[test]",
]

[envs.default.scripts]
test = "pytest {args:tests}"

[envs.dev]
dependencies = [
    "perturbo[test]",
    # For notebooks
    "ipykernel",
    "ipython",
    "seaborn",
    "scanpy",
    "matplotlib",
    "graphviz",
    # For reading R data files
    "anndata2ri",
    # For VSCode
    "black",
    "ruff"
]

[[envs.all.matrix]]
python = ["3.8", "3.10"]
