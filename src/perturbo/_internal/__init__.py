"""Implementation details of the conditional randomization test.

These modules are the CRT's kernels, its design construction and its tail
approximations. They are not a public interface and carry no stability promise;
:mod:`perturbo.crt` is the supported entry point. They live here rather than in the
research repository because the production test runs on them.
"""
